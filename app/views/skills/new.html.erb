<%= simple_form_for [@skill] do |f| %>
  <%= f.input :city %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit "add a skills", class: "btn btn-primary" %>
<% end %>
